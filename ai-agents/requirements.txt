# requirements.txt
# Core dependencies for Multi-Agent Marketing Orchestrator

# Flask for webhook server
flask==3.0.0
flask-cors==4.0.0

# Fetch.ai AgentVerse
uagents>=0.23,<0.25
uagents-core>=0.4,<0.5

# CrewAI and AI dependencies
crewai==0.28.8
crewai-tools==0.1.6
langchain>=0.1.10,<0.2.0
langchain-openai==0.0.5
openai>=1.13.3,<2.0.0

# Utilities — setuptools supplies pkg_resources (required by CrewAI/LangChain).
# Pin <82: setuptools 82+ removed pkg_resources (breaks those imports).
# Windows CMD + venv: venv\Scripts\activate.bat then python -m pip install -r requirements.txt
setuptools>=69.0.0,<82.0.0
python-dotenv==1.0.0
pydantic>=2.8,<2.9
requests>=2.32.3,<3.0

# Logging
loguru==0.7.2